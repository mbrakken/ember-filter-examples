import { resizedImage } from 'filter-examples/helpers/resized-image';
import { module, test } from 'qunit';

module('Unit | Helper | resized image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resizedImage([42]);
  assert.ok(result);
});
